<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10">
  <div>
    <h1 class="text-2xl font-semibold leading-7 text-gray-900 dark:text-white">Edit Calculated Field</h1>
    <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Update calculated field "<%= @calculated_field.name %>" for campaign "<%= @campaign.name %>"</p>
  </div>
  
  <div class="mt-8 border-t border-gray-200 dark:border-gray-700 pt-8">
    <%= render "form", campaign: @campaign, calculated_field: @calculated_field %>
  </div>
</div>