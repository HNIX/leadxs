<div class="container mx-auto px-4 py-6">
  <div class="flex items-center mb-6">
    <h1 class="text-2xl font-bold">
      <%= link_to @source.campaign.name, campaign_path(@source.campaign), class: "text-blue-500 hover:text-blue-700" %>
      <span class="text-gray-500">/</span>
      <%= link_to "Sources", campaign_sources_path(@source.campaign), class: "text-blue-500 hover:text-blue-700" %>
      <span class="text-gray-500">/</span>
      <%= link_to @source.name, source_path(@source), class: "text-blue-500 hover:text-blue-700" %>
      <span class="text-gray-500">/</span>
      Edit
    </h1>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render 'form' %>
  </div>
</div>